name: Build Node PAC

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 4320
    steps:
    - uses: actions/checkout@v2
    - name: Build Node
      run: ./build-node-docker-arm64.sh
